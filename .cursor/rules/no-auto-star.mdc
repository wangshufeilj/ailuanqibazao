---
description: 收藏操作规则：未经用户明确要求，不得擅自收藏或取消收藏文件
alwaysApply: true
---

# 收藏操作规则

## 一、基本原则

当用户没有明确说出“收藏”“加入收藏”“标记收藏”“取消收藏”等指令时，**不要主动为任何文件添加或删除收藏标记**。

## 二、执行要求

1. 只有在用户明确要求收藏或取消收藏时，才可以修改对应文件的 `starred`、`starred_date` 等字段。
2. 如果用户只是保存、整理、分类、写笔记或生成正文，**默认不视为收藏请求**。
3. 如果收藏对象不明确，先向用户确认目标文件，再执行。
4. 不要为了“顺手”或“补充知识库”自动创建 `collections/` 条目，除非用户明确要求收藏。

## 三、默认行为

- 用户发来文章、笔记、图片或链接时，默认只做分类、保存或整理。
- 用户未明确要求时，不新增收藏理由、不生成精选条目、不同步收藏索引。
- 发现已有收藏流程时，先停下来确认，再继续。

## 四、适用范围

本规则适用于仓库内所有可收藏的 Markdown 文件，尤其是 `reading/`、`articles/`、`news/`、`research/`、`reference/`、`chinese-documents/`、`social/`、`forum/` 下的内容。
