---
description: Markdown 双语/三语对照块硬换行规范（行尾两个空格）— 精读笔记必查
globs: "**/reading/notes/**/*.md"
alwaysApply: true
---

# Markdown 双语与三语块换行 Rule

当正文出现「双语」或「三语」逐句对照块时，统一使用行尾两个空格保证预览稳定分行。  
注意：符号不固定、顺序不固定，按语义识别为准。

## Agent 执行要求（必读 · 高优先级）

**凡是新建、编辑或整理 `reading/notes/` 下含多语种逐句对照的 Markdown（英/中、俄/中、西/英、俄/英/中、西/英/中等任意双语或三语组合），落稿前必须完成硬换行检查，不得遗漏。**

### 双语（两行一组）

- **必须**：在每一组双语对照的**第一行**行尾追加**两个空格**（Markdown 硬换行）。
- **必须**：第二行对照行保持原样即可（不强制加行尾双空格）。
- **建议**：全文写完后通读预览或快速扫描，确认两行不会糊成一段。

### 三语（三行一组）

- **必须**：在每一组三语对照的**前两行**行尾追加**两个空格**。
- **必须**：第三行对照行保持原样即可（不强制加行尾双空格）。
- **建议**：同上。

若用户未提及，Agent 仍默认按本规则处理；若与「纯原文、禁止改格式」等用户指令冲突，以用户明确指令为准。

## 适用场景

- **双语**：同一连续块中存在 2 行对照句，分别对应两种语言（如英/中、俄/英等）。
- **三语**：同一连续块中存在 3 行对照句，分别对应三种语言。
- 行首标记可不同（例如 `🔻/🔹`、`EN/ZH`、`俄/英` 等），不要求固定样式。
- 行顺序可变；重点是判断其是否为同一组对照内容，而不是判断符号本身。

## 排除场景

- 代码块（fenced code block）中的内容。
- 引用块（如 `> 🔻 ...`）中的内容。
- 列表项（如 `- 🔹 ...`）中的内容。
- 已使用 HTML 标签（如 `<p>`、`<div>`）控制排版的内容。
- 不是逐句对照的普通段落（即使出现多语言词汇，也不按对照块处理）。

## 执行规则（统一记法）

- **两行一组**：仅**第一行**行尾加两个空格。
- **三行一组**：**前两行**行尾加两个空格；第三行不强制。
- 若无法确定是否为对照块，先确认再处理，避免误改普通正文。

## 最小示例（Before / After）

### 双语

Before:

```text
EN: **English sentence**
中文：**中文句子**
```

After:

```text
EN: **English sentence**  
中文：**中文句子**
```

### 三语

Before:

```text
EN: **English sentence**
中文：**中文句子**
RU: **Русское предложение**
```

After:

```text
EN: **English sentence**  
中文：**中文句子**  
RU: **Русское предложение**
```
