---
description: 将暗号 3333 解释为三审三校
alwaysApply: true
---

# 暗号 3333

## 基本约定

当用户在同一条请求中明确写出 `3333` 或 **`三三三三`**（四个“三”）时，表示本次任务需要执行“**三审三校**”。

## 执行要求

- 将 `3333` / `三三三三` 视为用户的明确附加要求，不当作普通数字处理。
- 适用于本仓库内的文本整理、写入、润色、保存、衔接、笔记生成等任务。
- 在正式落稿前，默认多做一轮严格复查，重点检查：
  - 事实与前后表述是否明显矛盾；
  - 结构是否完整、衔接是否顺畅；
  - 错别字、漏字、重复字、标点和格式问题。
- **结合搜索与术语核对（3333 必做项）**：
  - 对文中**专名、政策用语、机构全称与简称、职务表述、日期与会议名称、数据与引文**等，在能力范围内用**检索**（如权威官网、通讯社、法规库、百科二次核对）做交叉验证；无法核实时在文中或注释中标明“待核”“以原文/正式文件为准”，不凭记忆写死不确定口径。
  - **术语优先**：党政、国资央企、法律与行业术语尤需核对标准表述，避免混用近义词或过时提法。
- 若发现低置信度修改、可能改变原意的地方，先询问用户，不擅自改写。

## 默认边界

- 仅在用户明确写出 `3333` 或 `三三三三` 时触发，不自动常驻到每一次任务。
- 若用户同时给出更具体的校对要求，以用户当次要求为准。

## 示例

- 用户说：“这篇按 `3333` 处理后再保存。” → 先完成正文整理，再**检索核对关键术语与专名**，最后按三审三校标准复查后落稿。
- 用户说：“`三三三三` 过一遍这篇里的国资委、新质生产力表述。” → 同上，并**优先**核对用户点名的术语。
