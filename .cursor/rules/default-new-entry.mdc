---
description: 同一对话默认接续规则：以对话内确立的目标为准；勿仅凭 IDE 当前打开文件判定；仅在明确要求时新建
alwaysApply: true
---

# 同一对话默认接续规则

## 零、编辑器打开文件 ≠ 本次任务目标（高优先级）

1. **用户当前在 IDE 中打开、聚焦的标签页，可能仅是上次编辑遗留未关**，与本轮对话任务**无必然关系**。
2. Agent **禁止**仅凭「系统提示里的 currently focused / open file」就假定：续写目标 = 该文件。
3. **判定本次写入/编辑目标文件时**，须按优先级综合判断（见第三节），**不得**把「当前打开文件」当作默认接续对象，除非用户在本轮对话中已明确把它定为目标，或内容与该文件强一致且对话已建立连续任务。

## 一、基本原则

当用户在**同一次对话**中继续输入内容时，默认视为“接续当前任务/当前文档”，优先在**本对话已确立目标**的文件上续写或追加，而不是新建文件。

## 二、何时新建文件

仅当用户明确表达以下意图之一时，才新建文件：

1. 明确说“新起一篇”；
2. 明确说“另存为新文件”；
3. 明确说“换一篇/不是接上文”并指向新目标。

## 三、执行要求（默认接续与目标判定）

1. **目标文件优先级（从高到低）**  
   - 用户在本轮消息中**写明路径、文件名、序号**（如「接到 `006`」「改 `reading/.../xxx.md`」）→ **必须**以该为准。  
   - 本对话中**已写入或已确认**的「当前任务文件」（例如用户连续粘贴同一篇材料且 Agent 已落库到某路径）→ **接续该文件**。  
   - 用户未指定路径、且对话中**尚未**产生任何目标文件时：按 `file-classification.mdc` **新建**合理路径下的文件；若粘贴主题与 IDE 当前打开文件**明显无关**，**禁止**写入该打开文件。  
   - 意图不清时 → **简短询问**用户要接到哪篇或是否新建，避免误写入。

2. 用户在同一对话继续输入内容，且未要求新建时，默认接续到**上述优先级确定**的当前目标文件，**不是**自动等同于 IDE 焦点文件。

3. 若用户明确指定“接到某篇/改某篇”，以用户指定文件为准。

4. 若用户明确要求新建，才进入新建流程，并遵循分类、命名、序号、frontmatter 规则。

5. 仅当意图明显冲突或目标不明时再询问确认，避免重复打断。

## 四、示例

- 用户说：“这个给我新起一篇” → 新建文件。
- 用户在同一对话继续粘贴下一段内容（未说新建），且本对话已确立目标文件 → 续写该目标文件（**不因 IDE 仍开着别的标签而改写到错误文件**）。
- 用户说：“把这段接到 008 后面” → 编辑 `008` 对应文件，不新建。
- IDE 仍打开 `018-某主题.md`，但用户新粘贴内容与该文件主题明显无关、也未说“接到这篇” → **不**写入 `018`；按分类新建或询问用户目标路径。
