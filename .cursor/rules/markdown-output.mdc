---
description: 生成 Markdown 文件的输出位置规范
alwaysApply: false
---
# Markdown 生成文件输出规范

## 工作流程

### 用户输入 Markdown 内容时的处理流程

当用户提供 Markdown 格式的内容时，遵循以下流程：

1. **用户输入**：用户提供 Markdown 格式的内容（可能是一大段文本、网页全选粘贴、长文、笔记等）
2. **AI 职责**：
   - 根据内容语义判断**主题**（参考 LCC 分类体系）及顶层类别
   - 确定存放路径（如 `reading/notes/{主题}/`、`articles/{主题}/`、`generated/` 等）
   - 查询目标子目录内现有序号，确定新序号（001、002…）
   - 根据内容提炼标题，按命名规范命名
   - **创建空 md 文件**（或仅含最简元信息头，如标题、来源、日期），存入正确位置
3. **用户职责**：md 文件正文由**用户自己复制粘贴**，AI 不负责填充原文内容，以加快处理速度
4. **命名规范**：遵循下方命名规则

**重要说明**：大部分情况下，用户会自行粘贴完整内容，AI 只需负责分类、确定路径、创建文件框架，无需填充正文内容。

## 输出位置

**所有由 AI 生成的 Markdown 文件必须保存到工作区的 `generated/` 文件夹。**

- 路径：`d:\AI驱动整理乱七八糟\generated\`
- 这是本工作区专门用于存放 AI 生成内容的特殊文件夹
- **generated/summaries/**：专门存放图片、视频等多媒体文件的 Markdown 摘要，便于检索

## 命名规范

文件名格式：**序号 + 标题 + 录入日期**

- **序号**：流水号（如 001、002）
- **标题**：内容主题，使用有意义的名称
- **录入日期**：文件创建/录入日期（建议格式：YYYY-MM-DD）

## 示例

```
generated/
  001-Russian-Oligarch-Lebedev-Notes-2025-03-08.md
  002-Project-Summary-2025-03-09.md
  003-Meeting-Notes-2025-03-10.md
```
