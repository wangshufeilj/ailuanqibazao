---
description: 暗号 1949101 表示政治理论类备考推荐标签
alwaysApply: true
---

# 暗号 1949101

## 基本约定

当用户在同一条请求中明确写出 **`1949101`** 作为**暗号**（与「备考」「打标签」「标记」等意图连用，或单独一句「`1949101`」指向当前文档）时，表示要为**目标 Markdown 文档**的 YAML frontmatter **`tags`** 字段**追加**（或确保包含）以下**固定标签**：

**`政治理论类备考推荐`**

## 执行要求

- 将 **`1949101`** 视为用户对「政治理论类备考推荐」这一分类/检索标签的**快捷指令**。
- **触发条件**（满足其一即可）：
  - 用户明确写出暗号 **`1949101`**，且意图为给笔记/文章打标签、分类或标记备考向；
  - 用户说「按 `1949101` 处理」「`1949101` 打上」等同义表达。
- **操作**：在目标文件 `tags` 列表中加入 **`政治理论类备考推荐`**；若已存在该标签，**不重复追加**。
- 若用户未指定文件且存在**当前对话已确立的落库/编辑目标文件**，默认对该文件操作；若无法确定目标，**先简短询问**用户要标记哪一篇。

## 默认边界

- 仅在用户以**暗号方式**使用 **`1949101`** 时触发；正文中的普通数字、年份、编号等**不**自动触发。
- 若用户同时给出其他标签或分类要求，与 **`政治理论类备考推荐`** **一并满足**。

## 与旧暗号的关系

- **`1949`** 已不再作为本仓库备考标签暗号使用（易与年份等混淆）；请统一使用 **`1949101`**。

## 示例

- 用户说：「这篇 `1949101`。」→ 给当前讨论的精读笔记的 `tags` 增加 **`政治理论类备考推荐`**。
- 用户说：「保存，顺便 `1949101`。」→ 落库后在 frontmatter 中带上该标签。
