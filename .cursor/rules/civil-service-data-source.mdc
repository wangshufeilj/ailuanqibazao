# 公考资料分析数据源

## 一、基本约定

根目录下的 **`公考资料分析数据源/`** 是一个**专题入口目录**，用于集中存放可作为公务员/事业单位考试「资料分析」题训练素材的统计公报、行业年报、经济数据等一手材料。

- 该目录**不取代原始分类**：文件仍按 `file-classification.mdc` 存放在 `chinese-documents/`、`reference/` 等原目录；`公考资料分析数据源/` **仅收录一份副本**。
- 原始归档与副本通过 frontmatter 的 **`origin.primary_md`** 字段互相指向，保持双向可追踪。

## 二、触发条件

当用户明确表述以下任一意图时，视为触发本规则：

- 「这是/也是公考资料分析数据源」
- 「打上公考资料分析数据源」「打公考数据源标签」
- 「把它加入（公考）数据源」
- 其他明确将某一文件标记为资料分析训练用数据源的表达

## 三、执行流程（Agent 必做）

收到触发信号后，按顺序完成以下三步，**缺一不可**：

### 3.1 在原文件追加标签

- 在**原文件**（含 PDF 配套的 `.md` 元信息文件，以及对应的精读笔记 `.md`，若存在）的 YAML frontmatter `tags` 中追加：

 **`公考资料分析数据源`**

- 若该标签已存在，**不重复追加**。
- 精读笔记与元信息文件**均需**打标签（便于两侧都能被检索到）。

### 3.2 复制副本到专题目录

将以下文件复制一份到 `公考资料分析数据源/`（**保持同名，不改序号、不改命名**）：

1. **文件本体**：PDF / XLSX / CSV 等原始数据文件；
2. **配套 `.md`**：PDF/二进制文件的元信息 md。

不要求复制精读笔记（`.md` 精读本身篇幅大且频繁更新，留在原路径即可；仅在原笔记 frontmatter 中打标签，便于反向检索）。若用户明确要求复制精读笔记，再按指令执行。

副本 md 的 frontmatter 需调整：

- `category: 公考资料分析数据源`
- 新增 `origin.primary_md`：指向 `chinese-documents/...` 下的原始 md
- 新增 `origin.reading_notes_md`（若有精读笔记）：指向精读笔记路径
- `media.path` 更新为副本所在路径

### 3.3 更新清单

在 `公考资料分析数据源/README.md` 的"清单"表中追加一行，字段包括：序号（在该表内独立流水）、标题、发布机构、原始路径（可点击链接）、本处副本文件名。

### 3.4 向用户汇报

汇报内容至少包含：

- **原文件**完整路径与可点击链接；
- **副本**完整路径与可点击链接；
- 已在哪些文件的 `tags` 中追加 `公考资料分析数据源`；
- 已更新清单。

## 四、二进制文件复制说明

- Markdown 等文本文件用 Read + Write 完成复制。
- PDF / XLSX 等二进制文件用系统级 `Copy-Item`（Windows PowerShell）或等价命令完成复制；这属于 `python-tech-stack.mdc` 第六节所述"系统级必需操作"的例外情形。
- **严禁**只更新 md 指向而不实际复制本体，或只复制本体而不更新 md。

## 五、边界

- 仅在用户明确触发时执行；**不**主动把看起来像统计数据的文件都放进来。
- 若触发时原文件不是 PDF 而是纯 md（如直接整理自网页），仅复制该 md 到 `公考资料分析数据源/`，并按 3.1 / 3.3 / 3.4 处理。
- 若同一批次涉及多个文件（如多份公报一次性标记），按同一流程批量执行，README 清单按条目追加。

## 六、30 篇细分规则同样适用（强制）

`公考资料分析数据源/` 虽是专题入口目录、独立于 LCC 主题映射，但**仍须遵守** `file-classification.mdc` 第二节"高流量 LCC 目录的混合细分原则"中的**30 篇阈值强制细分规则**：

1. **每次复制副本前**，Agent **必须**统计本目录内现有 `.md` 文件数（不计 `README.md`，其余副本 md 均计入；子目录建立后统计对应子目录内的 md 数）。
2. 若目标目录内 `.md` **≥ 30**：
 - **立即停手**，不执行复制、不追加清单、不自拟细分方案；
 - 仅向用户上报：哪个目录达到阈值、本次打算复制的文件是什么、备选细分维度建议（如按**年度** `2024/`、`2025/`，按**地域** `shanghai/`、`national/`，按**领域** `ocean-economy/`、`population/`，或按**发布机构**等）；
 - **等待用户决定**细分方案后再执行拆分与复制。
3. 用户确认方案后，Agent 按指令执行迁移与本次复制，并**同步在 `README.md` 清单中标注子目录分组**，以及在本规则文档或对应 README 中补充新增子目录说明。
4. 阈值规则**递归适用**：子目录再次达到 30 也同样触发，无层级上限；但整个仓库总深度仍受 `file-classification.mdc` 约束（不超过 4 层）。

副本归入子目录后：

- 原文件 `tags` 打标签的动作**不变**；
- 副本 md 中 `media.path`、`origin.primary_md` 等相对路径须相应更新；
- 清单行的"本处副本"字段写明子目录 + 文件名，便于检索。

## 七、与其他规则的关系

- 与 `no-auto-star.mdc` 一致：**不自动**触发，仅在明确指令下执行。
- 与 `file-classification.mdc`："深度不超过 4 层"约束并行遵守；**30 篇阈值细分规则同样适用**（见第六节）；本目录独立于 LCC 主题映射，按专题组织。
- 与 `git-commit-style.mdc` 结合:提交此类变更时可使用 `notes(civil-service-data-source): add <topic> as exam data source [YYYY-MM-DD]` 或 `docs(civil-service-data-source): ...` 风格。
