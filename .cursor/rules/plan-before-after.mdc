---
description: 做计划时每条改动须包含「修改前 / 修改后」对照
alwaysApply: true
---

# 计划中的修改前/修改后规范

当 Agent 使用 CreatePlan 或书面列出多步修改方案时，**对每一项具体改动都要明确写出「修改前（Before）」与「修改后（After）」**，让用户一眼看到改动的效果。

## 要求

- **每条修改项**下须包含：
  - **修改前（Before）**：当前状态 / 原文 / 现有结构或行为（可摘关键片段或概括）。
  - **修改后（After）**：修改完成后的状态 / 新文 / 新结构或行为（可摘关键片段或概括）。
- 若改动涉及多行或整段，可用代码块或列表简要展示前后差异；若为结构性/行为性改动，用简短描述说明「之前怎样 → 之后怎样」即可。
- **全部改动**都要带上 Before/After，不要只写「修改建议」而不写前后对照。

## 目的

- 用户能快速判断方案是否符合预期，减少误改。
- 执行前即可预知每处改动的具体效果，便于确认或调整。

## 示例（衔接类）

**问题**：文首有多余句与分隔线。

- **修改前（Before）**：frontmatter 与「# 模块一」之间有一句「现在我有足够的信息……」及 `---`。
- **修改后（After）**：frontmatter 后直接是「# 模块一：翻译与全文概要」，无多余句与分隔线。
