---
description: 当用户提供网址时，默认使用 user-jina-mcp-server 抓取并存放处理后的原文及相关信息。
alwaysApply: true
---

# Jina MCP 网址处理默认策略

当用户提供网址时，一般**期望**：使用 `user-jina-mcp-server` 抓取并处理网页，**存放** Jina 处理过的原文和相关信息。

当任务涉及网址抓取、网页正文提取、在线内容检索时，默认**优先使用 `user-jina-mcp-server`**。

## 一、例外情况

仅在以下场景可考虑非 Jina 方案：

1. Jina 访问失败或返回内容明显不完整；
2. 用户明确要求使用其他工具；
3. 任务目标并非网页内容获取（如本地文件操作）。

## 二、执行要求

1. **抓取与存放**：用 Jina 提取正文后，按 `file-classification` 规则将处理过的原文及相关信息（标题、来源、日期等）入库保存；
2. 在不影响准确性的前提下，优先保留原文关键信息；
3. 补充内容应简洁、可检索，不引入无依据推断；
4. 若原文语种不明确，先说明语种判断依据，再给出中英文补充。