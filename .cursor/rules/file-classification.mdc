---
description: 多格式文件分类规则：目录结构、命名规范、多媒体处理流程
alwaysApply: true
---

# 多格式文件分类体系

当用户提供各类文件（Markdown、图片、视频、Word、表格等）时，按本规则进行分类、存储与处理。内容主要来自互联网，支持多语言、多类型。

## 一、目录结构

**内容类顶层目录 14 个**：articles、news、research、tutorials、reference、chinese-documents、social、**forum**、reading、creative、images、videos、data、audio；每类深度不超过 **4 层**。另含 **inbox**（待分类入口）、**collections**（精选收藏，脚本自动生成）、**generated**（AI 生成内容，见「六、其他说明」）。主题分类采用美国国会分类法（LCC），见下方「主题分类体系」。

```
d:\AI驱动整理乱七八糟\
├── inbox/                    # 待分类入口
├── articles/                 # 文章（博客、专栏、长文）
│   ├── general/              # 综合/总论
│   ├── philosophy/           # 哲学、心理学、宗教
│   ├── history-auxiliary/    # 史学辅助（考古、档案等）
│   ├── history/              # 历史（含中国特色子类）
│   │   ├── world/
│   │   ├── china/
│   │   │   ├── tongshi/      # 通史
│   │   │   ├── duandaishi/   # 断代史
│   │   │   ├── revolutionary/  # 革命史
│   │   │   ├── cultural/     # 文化史
│   │   │   ├── local/        # 地方史志
│   │   │   └── ethnic/       # 民族史志
│   │   ├── europe/
│   │   ├── americas/
│   │   ├── asia/
│   │   └── other/
│   ├── geography/            # 地理、人类学、休闲
│   ├── social-sciences/      # 社会科学（经济、社会学）
│   ├── politics/             # 政治
│   ├── law/                  # 法律
│   ├── education/            # 教育
│   ├── arts/                 # 艺术、音乐
│   ├── language-literature/  # 语言与文学
│   ├── science/              # 科学
│   ├── medicine/             # 医学
│   ├── agriculture/          # 农业
│   ├── technology/           # 技术
│   ├── military/             # 军事
│   ├── bibliography/         # 文献、信息科学
│   └── other/
├── news/                     # 新闻资讯（其下主题子目录同 articles，采用 LCC 映射）
├── research/                 # 研究/学术
│   ├── papers/               # 论文，其下按主题分子目录
│   └── other/
├── tutorials/                # 教程/指南（其下主题子目录同 articles，采用 LCC 映射）
├── reference/                # 参考资料（docs、other 下可按 LCC 主题分子目录）
│   ├── docs/                 # 文档
│   └── other/
├── chinese-documents/        # 中文文档（中文内容优先归入，不局限于官方来源）
│   ├── party/                # 党的文件（理论创新、党内文件等）
│   ├── government/           # 政府机关（国务院、各部委、地方政府等）
│   ├── legislature/         # 人大（全国人大、地方人大等）
│   ├── judiciary/            # 司法机关（法院、检察院等）
│   ├── military/             # 军队（中央军委、各军种等）
│   ├── state-owned/          # 国有企业（央企、地方国企等）
│   ├── public-institutions/  # 事业单位（学校、医院、科研院所等）
│   ├── social-organizations/ # 社会组织（协会、基金会、商会等）
│   ├── companies/            # 公司（民营企业、上市公司等）
│   ├── international/        # 国际组织（联合国、国际机构等，涉及中国相关）
│   ├── recruitment/         # 招聘公告（公务员、事业单位、国企等）
│   ├── central-media/        # 中央媒体（新华社、人民日报、央视、央广、光明日报、经济日报、中国日报等）
│   ├── local-media/          # 地方媒体（各省市党报、地方电视台、地方新闻网站等）
│   ├── other-media/          # 其他媒体（商业媒体、行业媒体等）
│   └── other/                # 其他（公文、通告、通稿、新闻、教程、民间文档等）
├── social/                   # 社交媒体/讨论
│   ├── posts/                # 帖子：先按平台分子目录，再按 LCC 主题分子目录（见「social/posts 平台细分」）
│   │   ├── reddit/
│   │   │   └── education/    # 示例：reddit + 主题
│   │   ├── xiaohongshu/
│   │   │   └── technology/   # 示例：小红书 + 主题
│   │   └── other/            # 平台不明或小众平台可暂放 other/{主题}/
│   ├── discussions/          # 讨论，其下按主题分子目录
│   └── other/
├── forum/                    # 外文帖子/论坛/平台信息（其下按 LCC 主题分子目录）
│   ├── general/
│   ├── technology/
│   ├── politics/
│   ├── law/
│   ├── education/
│   ├── science/
│   └── other/                # 其余主题同 LCC，按需创建
├── reading/                  # 阅读笔记/书摘
│   ├── notes/                # 笔记，其下按主题分子目录
│   ├── excerpts/             # 摘录，其下按主题分子目录
│   └── other/
├── images/                   # 图片（格式子目录下可按主题分）
│   ├── screenshots/
│   ├── diagrams/
│   ├── photos/
│   └── other/
├── videos/                   # 视频
│   ├── tutorials/
│   ├── recordings/
│   └── other/
├── data/                     # 数据/表格
│   ├── spreadsheets/
│   ├── datasets/
│   └── other/
├── audio/                    # 音频
│   ├── podcasts/
│   └── other/
├── collections/              # 精选收藏（脚本自动生成条目，无索引）
├── creative/                 # 创意/文学
│   ├── writing/              # 写作，其下按主题分子目录
│   └── other/
└── generated/                # AI 生成内容
    └── summaries/            # 多媒体摘要
```

---

## 二、主题分类体系（LCC）

采用美国国会分类法（Library of Congress Classification）映射为主题目录，各类别复用：

| LCC 类 | 主题目录名 | 中文说明 |
|--------|------------|----------|
| A | general | 综合/总论 |
| B | philosophy | 哲学、心理学、宗教 |
| C | history-auxiliary | 史学辅助（考古、档案等） |
| D–F | history | 历史（世界、欧美、亚非等） |
| G | geography | 地理、人类学、休闲 |
| H | social-sciences | 社会科学（经济、社会学） |
| J | politics | 政治 |
| K | law | 法律 |
| L | education | 教育 |
| M–N | arts | 艺术、音乐 |
| P | language-literature | 语言与文学 |
| Q | science | 科学 |
| R | medicine | 医学 |
| S | agriculture | 农业 |
| T | technology | 技术 |
| U–V | military | 军事 |
| Z | bibliography | 文献、信息科学 |

### 历史类中国特色（参考中国图书馆分类法 K 类）

`history/china/` 下增设：

| 子目录 | 中文说明 |
|--------|----------|
| tongshi | 通史 |
| duandaishi | 断代史（先秦、秦汉、隋唐、宋元、明清、近代等） |
| revolutionary | 革命史 |
| cultural | 文化史 |
| local | 地方史志 |
| ethnic | 民族史志 |

### 教育类中国特色

`education/` 下增设：

| 子目录 | 中文说明 |
|--------|----------|
| civil-service-exam | 考公考编备考资料（公务员考试、事业单位考试、教师编制考试等） |

### 中文文档类特色

`chinese-documents/` 用于存放**中文为主**的各类文档，**不局限于**党政机关、国企、事业单位等官方来源，可包括但不限于：

- 公文、通告、通稿、新闻（党政机关、国企、事业单位等）
- 招聘公告、政策解读、法规条文
- 民间机构、媒体、个人发布的中文文档
- 教程、手册、说明书等中文资料

子目录按**发布主体/来源**划分：

| 子目录 | 说明 | 典型来源示例 |
|--------|------|-------------|
| `party/` | 党的文件 | 中共中央、中组部、中宣部等 |
| `government/` | 政府机关 | 国务院、各部委、地方政府等 |
| `central-media/` | 中央媒体 | 新华社、人民日报、央视、央广、光明日报、经济日报、中国日报等 |
| `local-media/` | 地方媒体 | 各省市党报、地方电视台、地方新闻网站等 |
| `other-media/` | 其他媒体 | 商业媒体、行业媒体等 |
| `other/` | 其他 | 无明确主体或混合来源时归入 |

**媒体分类说明**：
- **中央媒体**：由中央直接管理或主管的新闻媒体机构
- **地方媒体**：由省、市、县等地方党委政府管理的地方新闻媒体
- **其他媒体**：商业媒体、行业媒体等（不含自媒体和境外中文媒体）

### 可扩展性

若内容无法归入现有主题，可在对应类型下**新建主题子目录**，命名采用小写英文连字符；新建后需更新本规则文档的类别说明。

### 论坛与平台类（forum）

**forum/** 用于存放**外文的帖子、论坛讨论、平台公告/说明**等（如 Cursor 论坛、GitHub Discussions、Discord 公告、英文社区帖等）。其下按**主题**分子目录（与 LCC 映射一致，如 technology、politics、law、education、science 等）。  
**此类内容不放入 reading/notes/**：即便带精读/笔记形式，也归入 forum 对应主题下，以与「阅读笔记」区分。

---

## 三、分类原则

1. **先按内容语义选主题**（LCC 映射），再按格式归入对应顶层类别。
2. 按内容语义归类，与文件格式、语言无关。
3. 无法判断主题时，归入对应顶层下的 `other/`。
4. 无法匹配现有主题时，可新建主题子目录（见上文「可扩展性」）。
5. **中文内容优先**：**中文为主**的文档（公文、通告、通稿、新闻、教程、民间资料等），**优先考虑**归入 `chinese-documents/`；若明显属于其他顶层类别（如学术论文、多媒体等）再归入对应类别。不局限于党政机关、国企、事业单位等官方来源。
6. **外文帖子/论坛/平台信息**：来自外文论坛、社区、产品官方讨论区、平台公告等的帖子/讨论/说明，**一律归入 forum/**，其下按主题分子目录；**不放入 reading/notes/**。
7. **社交媒体帖子（social/posts）路径**：`social/posts/{platform}/{主题}/`。先判定**来源平台** `{platform}`（见下表，目录名用小写英文连字符），再按内容语义选 **LCC 主题** `{主题}`；**序号在 `social/posts/{platform}/{主题}/` 内独立流水**。frontmatter 中 **`category` 与完整相对路径一致**（如 `social/posts/reddit/education`），并推荐增加 **`platform`** 字段（与 `{platform}` 同名），便于检索。

**`{platform}` 常用取值（可扩展）**

| 目录 slug | 平台说明 |
|-----------|----------|
| `reddit` | Reddit |
| `xiaohongshu` | 小红书 |
| `zhihu` | 知乎 |
| `weibo` | 微博 |
| `douyin` | 抖音 |
| `bilibili` | Bilibili |
| `twitter-x` | X（原 Twitter） |
| `linkedin` | LinkedIn |
| `instagram` | Instagram |
| `facebook` | Facebook |
| `threads` | Threads |
| `telegram` | Telegram |
| `other` | 已知平台但上表未列、或需单独归类的来源 |
| `unknown` | 暂无法判断平台（后续可人工改 slug 并移动文件） |

**与 `forum/` 的区分**：`social/posts` 侧重**社交平台上的帖子/动态**（含中文平台）；`forum` 侧重**论坛长帖、社区公告、产品讨论区文档**等外文语境内容。若边界模糊，以「是否典型 SNS 信息流帖子」为准。

8. **「精读笔记」不是路径标签**：文件名或正文含「精读笔记」「阅读笔记」等，**不得**据此默认归入 `reading/notes/`。须**先行判定**应属哪一**顶层类别**：例如原文主体为新华社/人民日报等中央媒体或党政机关通稿、政策解读 → `chinese-documents/` 下对应子目录；外文论坛/产品讨论区长帖 → `forum/`；社交平台动态 → `social/posts/`；通讯社/报刊深度稿且按文章归档 → `articles/` 或 `news/` 等。**仅当**内容为个人对某文的阅读学习整理，且按来源与体裁**没有**更优先的顶层归宿时，再归入 `reading/notes/{LCC主题}/`。

   **Agent 易错反例（须避免）**：**省党报**（如《新华日报》《浙江日报》）及**人民网等地方频道**对招考、就业、民生政策的报道，即便整理为「精读笔记」、正文涉及事业单位或考编，仍按**发布来源**归入 `chinese-documents/local-media/`（若体裁为机关/事业单位**正式招聘公告原文**且以公告归档为主，可归 `chinese-documents/recruitment/`）。**禁止**仅因话题像「考编」「事业单位」就放入 `reading/notes/education/civil-service-exam/` 或仅凭 LCC「教育」主题覆盖中文媒体稿。

### 分类理由说明（Agent 必报）

**在确定路径并完成写入（或给出拟定路径）后，Agent 须用简短中文说明本次分类依据**，便于用户复核与日后调整规则。说明与路径展示**配套出现**，不得只给路径不给理由。

**应交代清楚（可合并为 2～5 句）：**

1. **顶层类别**：为何归入该顶层（如 `reading/notes/` 与 `chinese-documents/`、`forum/`、`articles/` 等）——依据**体裁/是否个人笔记/来源类型**，引用本节原则中 relevant 条款即可。
2. **LCC 主题目录**：为何选该主题子目录（如 `social-sciences` 与 `technology`、`politics` 的取舍）——依据**正文主旨**（财经/法律/工程技术/政治等），而非仅凭行业标签（例如「游戏公司」不等于必归 `technology`）。
3. **（可选）易混淆项**：若存在明显备选路径，用一两句说明**为何未采用**。

**篇幅**：精炼为主，避免冗长论证。**用户已明确表示「不必解释分类」时**，可仅报路径。

## 四、命名规范

统一格式：`序号-标题-日期.扩展名`

- **序号**：在各自子目录内流水编号（001、002…）
- **标题**：内容主题，支持多语言（英文连字符或中文）
- **日期**：录入日期 YYYY-MM-DD

| 类型 | 示例 |
|------|------|
| 文档 | `001-Russian-Oligarch-Lebedev-Notes-2025-03-08.md` |
| 图片 | `001-Product-Screenshot-2025-03-08.png` |
| 视频 | `001-Meeting-Recording-2025-03-08.mp4` |
| 表格 | `001-Sales-Data-Q1-2025-03-08.xlsx` |

## 五、处理流程

### 5.1 默认原则

**默认生成完整 md 内容**；仅当用户明确说「只创建空文件」「我自己粘贴」或使用暗号 **12306** 时，才创建空文件。

### 5.2 暗号速查表

| 暗号 | 路径 | 内容 |
|------|------|------|
| 无 / 10086 | 按主题分类（LCC 映射） | 生成完整内容 |
| 95533 | 固定 `education/civil-service-exam/` | 生成完整内容 |
| 12306 | 按主题分类（LCC 映射） | 仅创建空文件 |

### 5.2.1 阅读笔记元信息头规范

创建或写入 `reading/notes/` 下笔记时（含 12306 空文件），**统一使用较丰富的元信息头**，便于检索与关联。推荐字段如下（必填以 ★ 标注，其余按需填写）：

| 字段 | 说明 | 示例 |
|------|------|------|
| **title** ★ | 笔记/文章标题，与正文一致 | `路透社独家：中国第二大芯片制造商筹备7纳米生产` |
| **source** ★ | 文章来源/机构 | `Reuters`、`央视新闻` |
| **source_url** | 原文链接（若有） | 留空或 URL |
| **author** / **authors** | 作者（单人用 author，多人用 authors 数组） | `Fanny Potkin`、`朱江、张丛婧` |
| **date** ★ | 原文发布日期或笔记录入日期 | `2026-03-17` |
| **created_date** | 笔记创建/录入日期（与 date 不同时可写） | `2026-03-17` |
| **category** | 与存放路径一致，便于脚本/索引 | `reading/notes/technology` |
| **tags** | 主题标签（中文为主），便于检索 | `半导体`、`7纳米`、`华虹`、`英语精读` |

- **tags 尽量打丰富一些**：覆盖主题、行业、体裁、用途等多维度（如：主题词、机构/人物、技术节点、考试/学习用途等），便于日后按标签筛选与关联；数量根据文章具体情况确定，不设上限或下限。
- 空文件（12306）时：至少包含 **title**、**source**、**date**、**category**、**tags**，正文留空。
- 生成完整内容时：同上，并可在正文开头再写一遍「来源 / 题目 / 作者背景」等说明。

### 5.3 用户提供文本内容保存（统一流程）

**默认约定（重要）**：用户以 Markdown 形式输入、粘贴或分享内容时，**默认视为请求保存**，无需额外说出「保存」「帮我保存」等。Agent 应直接按本流程执行入库；仅当用户明确表示「仅讨论」「不保存」或类似意图时，才不执行保存。

当用户提供 Markdown、网页全选粘贴、长文、笔记、摘录等内容时（含上述默认视为保存的情形）：

1. **判断模式**：用户使用暗号 12306 或明确要求空文件 → 仅创建空文件；否则 → 生成完整内容
2. **确定路径**：**先**按第三节「分类原则」判定顶层类别（含原则 5～8：中文来源、`forum`、`social/posts`、**勿仅凭「精读笔记」字样**等）；暗号 95533 → `reading/notes/education/civil-service-exam/` 等考公目录；其余情况 → 在已定顶层类别下按 LCC 选子目录（如 `chinese-documents/central-media/`、`reading/notes/{主题}/`、`articles/{主题}/`、`social/posts/{platform}/{主题}/`）
3. **查询序号**：目标子目录内流水号（001、002…）
4. **提炼标题**：按 `序号-标题-日期.md` 命名
5. **写入**：生成完整内容（元信息头 + 正文，含 Markdown 格式）或创建空文件（**标准元信息头**，见 5.2.1）
6. **分类理由**：按第三节「分类理由说明（Agent 必报）」向用户简要说明本次路径选择依据（与文件链接或路径一并给出）

**PDF 等大文件**（如 research 论文）：同目录下创建同名 `.md` 元信息文件。

### 5.3.1 用户仅提供网址（URL）时的默认处理

当用户给出的是一个网址（无论是否附带少量说明）时，一般优先按以下顺序处理：

1. **先提取正文**：抓取页面可读正文（标题、作者/来源、发布日期、主体内容），尽量去除导航、广告、评论区、页脚等噪音。
2. **再整理为 Markdown**：将正文整理成结构清晰的 Markdown（含元信息头 + 正文分节），并保留 `source_url` 字段。
3. **最后按本规则入库**：继续遵循 5.3 的分类、命名、序号与写入流程，存入对应目录。

补充约束：

- 若页面无法访问、存在登录墙/反爬限制或正文提取失败，应明确告知用户，并请求补充原文文本或可访问链接。
- 若用户明确要求“仅保存链接不提取正文”，则按用户要求执行，不强制正文提取。

### 5.4 收藏流程

当用户对**已存在的** Markdown 文件明确说「收藏这篇」「收藏这个文件」或「取消收藏」时，才进入收藏处理；如果对象不明确，必须先向用户确认目标文件。

1. **收藏时**：
   - 先确认用户确实要执行收藏，而不是仅仅保存、整理或分类。
   - 在目标文件的 YAML frontmatter 中添加 `starred: true`、`starred_date: YYYY-MM-DD`（当日）。
   - 如用户明确要求同步收藏条目，再运行 `python scripts/generate_collections.py`。
   - 如需生成「收藏理由」，必须基于目标文件内容和已存在的项目知识库来写，不得凭空补充。

2. **取消收藏时**：
   - 删除目标文件 frontmatter 中的 `starred` 与 `starred_date` 字段。
   - 如用户明确要求同步移除条目，再运行 `python scripts/generate_collections.py`。

3. **默认约束**：
   - 未明确提到“收藏”时，不新增收藏理由，不生成精选条目，不同步收藏索引。
   - 任何收藏相关操作都应优先按 `no-auto-star.mdc` 的保守原则处理。

4. **适用**：`reading/notes/`、`social/posts/`、`forum/`、`articles/`、`news/`、`research/`、`tutorials/`、`reference/`、`chinese-documents/` 等下的 Markdown 文件。

### 5.5 多媒体（images / videos / audio）

1. 判断主题及格式，存入对应子目录
2. 按命名规范重命名
3. **生成 Markdown 摘要**，存入 `generated/summaries/`（元信息、内容描述、原文件链接、标签）
4. **在同目录下创建同名 `.md` 元信息文件**：包含标题、类型、日期、原文件名、摘要链接（若有），便于 Git 仅提交条目、不提交大文件

**涉及照片/图片时的处理规则**：

**触发条件（默认保存）**：用户在对话框中**直接发送图片**时，默认视为“需要保存图片本体到仓库”，应立即执行图片保存流程（本体存放 + 重命名 + md 更新）。仅当用户在同一请求中明确表示「仅讨论/识别，不保存」时，才不落库本体。

**图片随附文字的默认解释**：当用户与图片同时发送文字说明（如人物判断、时间推断、来源线索、命名偏好、标签建议）时，默认将该文字视为**补充信息**，用于完善文件命名、元信息与内容判断；除非用户明确说“不保存”，否则这些文字**不改变默认保存本体**的执行。

**保存流程（须一次完成三步）**：

1. **本体存放**：复制/移动图片到选定目录（`assets/`、`images/{主题}/` 等），确保文件实际存在。
2. **重命名**：按 `序号-标题-日期.扩展名` 命名，不保留 Cursor 或系统生成的冗长文件名。
3. **相关 md 更新**：在 frontmatter `media.path`、正文 `![]()` 等处填写新路径，与本体一致。

若任一步骤缺失，视为未完成；不得「只改 md 不落实现体」或「只存不重命名」。

**存放位置选择**：与某文档强关联（如档案影像对应某 chinese-documents 条目）时，优先 `assets/`（项目根或该文档同级）；独立图片、可按主题归类时，存入 `images/{主题}/`。

**反例与禁止**：

- 禁止：在用户已明确表示「不保存本体」后，仍将图片写入仓库。
- 禁止：保存时只复制不重命名，沿用 `c__Users_...workspaceStorage_...uuid.png` 等系统生成名。
- 禁止：只更新 md 路径而不确保图片本体存在于该路径。

### 5.6 数据类（data）

1. 判断主题及子类别（spreadsheets / datasets / other）
2. 存入对应目录，按命名规范重命名

摘要文件命名：`{序号}-{原文件标题}-summary-{日期}.md`

- **序号**：在 `generated/summaries/` 目录内独立流水编号（001、002、003…）
- **不继承原文件序号**，避免多个源文件（如 images、videos 中各自 001）生成摘要时序号冲突

### 5.7 文本衔接修复（按需触发）

当 Markdown 文件由多次粘贴形成、出现重复段或结构断裂时，衔接修复仅在以下两种情况触发：

1. **对话触发**：用户在当前对话中明确提出「做衔接」「衔接一下」「整理衔接」等指令。
2. **文件触发**：用户明确给出目标文件（当前文件或指定路径）并要求衔接。

**执行流程**（必须先确认后修改）：

1. **检测并定位**：扫描目标文件，找出粘贴痕迹、重复段、结构断裂等问题的具体位置（行号或段落描述）。
2. **输出修改方案**：向用户明确列出每处修改的位置、原内容与拟修改内容，以「修改位置 + 修改说明」形式呈现。
3. **等待用户确认**：在用户确认方案后再执行修改；用户可要求调整或跳过某处。
4. **执行并反馈**：修改完成后简要说明实际改动位置及结果。

执行原则（自适应修复）：

- 修复强度不固定为“保守”或“激进”，需根据上下文完整度、重复程度、结构断裂程度动态判断。
- 轻度问题（格式噪音、空行、分隔线重复）优先做轻修复：清理冗余、保持原文不动。
- 中度问题（同编号重复段、同标题“续”段、粘贴痕迹）做结构合并：去重并补齐缺失字段，保证段落连续。**两处可能都不完整**：最终采用的合并方案是**从两处中选取最完整内容拼成一条**——例如 A 处拓展完整、B 处例句完整，则合并为“A 的拓展 + B 的例句”；若仅一处某字段完整则取该处。目标是合并后的单条是两处所能拼出的**最完整版本**，再删除重复块。
- 重度问题（跨段错位、前后逻辑断裂、时间线混乱）允许适度重组顺序并补短衔接句，但不得改变事实与核心观点。
- 若存在低置信度语义冲突或多种合理解读，先提示用户确认后再修复，不擅自定稿。

## 六、其他说明

**generated/ 的职责**：`generated/` 仅用于 `generated/summaries/`（图片、视频、音频等多媒体的 Markdown 摘要）。笔记、文章、新闻等用户输入内容按主题存入 `reading/notes/{主题}/`、`articles/{主题}/`、`news/{主题}/` 等对应顶层目录，**不**存入 `generated/`。

**inbox**：`inbox/` 为待分类入口，可人工暂存待整理文件。当用户要求「对 inbox 中的文件进行分类」时，读取 inbox 内文件，按内容语义判断主题，移动到对应顶层目录，并按命名规范重命名。

**序号校验**：各子目录内序号须连续、无重号；`generated/summaries/` 内序号独立流水，不与源文件序号挂钩。

**大文件与空目录**：大文件（图片、视频、PDF、音频等，不含表格）不提交 Git，在同保存位置创建**同名 `.md`** 元信息文件即可。表格（xlsx、csv 等）可直接提交。空目录需放入 `.gitkeep` 以便 Git 追踪目录结构。

**INDEX.md 说明**：本项目不再维护 INDEX.md 索引文件。所有文件分类和创建工作照常进行，但不再更新索引。
